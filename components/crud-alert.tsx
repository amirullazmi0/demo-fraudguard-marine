export function CrudAlert({ message }: { message: { type: "success" | "error"; text: string } | null }) { if (!message) return null; return <div className={`fixed right-4 top-4 z-50 w-[calc(100vw-2rem)] max-w-md rounded-xl border px-4 py-3 shadow-lg text-sm font-medium ${message.type === "success" ? "border-emerald-200 bg-emerald-50 text-emerald-700" : "border-rose-200 bg-rose-50 text-rose-700"}`}>{message.text}</div>; }
